export class CuartosDisponibles {
  idTipoCuarto: number = 0;
  fechaInicioReserva: Date | null = null; // Representa LocalDate como una cadena en formato ISO (yyyy-MM-dd)
  fechaFinReserva: Date | null = null; // Representa LocalDate como una cadena en formato ISO (yyyy-MM-dd)
}
