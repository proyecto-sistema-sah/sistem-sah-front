import { Routes } from '@angular/router';
import { RegistrarUsuarioComponent } from './registrar-usuario.component';

export const REGISTRAR_USUARIO_ROUTES: Routes = [
    {
        path: '',
        component: RegistrarUsuarioComponent
    }
]