import { Routes } from '@angular/router';
import { LoginComponent } from './login.component';

/**
 * Definición de rutas para el módulo de inicio de sesión.
 */
export const LOGIN_ROUTES: Routes = [
  {
    path: '',
    component: LoginComponent
  }
];
